import React from 'react'
import DataTable from 'DataTable'
const compee = React.createClass({

  getInitialState() {
    this.setState({ii: dd})
  },
  
  componentDidMount() {
    
  },
  
  getInitialState() {
    get 
  }

  componentWillMount() {
    let arr = []
    arr.map((itme) => {
      if (itme) {
        
        }
      }
    })
  }
});

const dd = React.creactClass({
  render() {
    return (
      <div className="function-dd">
        {this.props.children}
      </div>
    );
  }
})

module.exports = compee;